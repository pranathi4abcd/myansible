---
- hosts: 172.31.35.213
  become: yes
  tasks:
  - name: install linux directly
    command: hostname -f 
    Register: result

  - name: view the first task result 
    debug:
     msg: "{{Register}}"  
    
     